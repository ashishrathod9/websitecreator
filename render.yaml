services:
  - type: web
    name: website-creator
    env: node
    buildCommand: |
      npm install
      cd backend && npm install
    startCommand: |
      npm run build
      cd backend && npm start
    envVars:
      - key: NODE_ENV
        value: production 