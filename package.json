{
  "name": "websitecreator",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "install-all": "cd college-website-generator && npm install",
    "build": "cd college-website-generator && npm run build",
    "start": "cd college-website-generator && npm run serve"
  },
  "engines": {
    "node": ">=18.0.0"
  }
} 